{
    "extends": "../../tsconfig/react-library.json",
    "include": ["**/*.ts", "**/*.tsx"],
    "exclude": ["node_modules", "build/**/*"],
    "compilerOptions": {
        "outDir": "build"
    }
}
